# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Personal / working files — not meant for a public repo
/*.pdf
/PLANNING_NOTES.md
/drafts/

# Loose images dropped in the repo root while working. Anything the site
# actually serves lives under public/, so nothing at the top level ships.
/*.jpg
/*.jpeg
/*.png
/*.JPG
/*.JPEG
/*.PNG
/*.webp
/*.HEIC

# Superseded by yosemite-1280.mp4 (9.7 MB) and yosemite-preview.mp4.
# The 20 MB original stays on disk but is no longer deployed.
/public/video/yosemite.mp4
